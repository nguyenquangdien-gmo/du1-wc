version: '3.8'

services:
  app:
    build: .
    container_name: wc2026_app
    ports:
      - "8080:8080"
    volumes:
      - ./wc2026.db:/app/wc2026.db
      - ./wiki:/app/wiki
    env_file:
      - .env
    restart: always
